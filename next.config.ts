import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",
  // basePath: process.env.NODE_ENV === "production" ? "/my-store-next" : "",
  // images: {
  //   unoptimized: true,
  // },
  // reactStrictMode: true,
  /* config options here */
};

export default nextConfig;
